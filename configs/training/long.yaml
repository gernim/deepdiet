# Long training configuration
# Use: python src/train_hydra.py training=long

epochs: 50
batch_size: 8
lr: 1e-4
weight_decay: 1e-4

lr_decay_epochs: 10
lr_decay_factor: 0.5
max_grad_norm: 5.0

num_workers: 4